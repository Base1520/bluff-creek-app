# Launch readiness · September 9, 2026

This feature branch consolidates the public app work already on `main` with Creek Office. It preserves the original Homestead brand and `/brand` library. Staff access and the reviewed app release have not been activated.

Verified office prerequisite recorded September 7: all six unchanged migrations applied to an isolated local Supabase stack; 38 actual Auth/REST/Storage checks passed with no local advisor findings. Captured local email and browser journeys passed eight password-reset, seven invitation/first-password, and seven public-signup checks. At that checkpoint, the full staff suite passed 196/196, public suite 57/57, and setup preflight 16 tests/41 checks. This is fictional local-service evidence; hosted activation, real email delivery, production recovery and actual phones remain separate gates. See the [recorded rehearsal](../tools/office-rehearsal/results-2026-09-07.md).

Current September 9 candidate: **283/283 staff tests and 69/69 public tests pass**. Focused coverage includes 56 session/editor tests, 33 membership-history tests, 25 office-content tests and 17 public-connection tests. Known unresolved writes retain their draft and original retry identity; late settlements require fresh recovery, and a profile read cannot overwrite a newer submission. Office discard decisions recheck their original dialog and current access. The source preflight remains 43/43. These are local code checks; actual hosted staff, native browser/phone and operator acceptance remain open. The unchanged public shell remains `creek-v12`; the modified connection script is excluded from that cache.

The separate current seven-migration local Auth/PostgREST rehearsal passes ten actual milestones plus 31 preparation/guard tests. It contains no uploaded file and does not extend the historical six-migration database/file restore evidence. Both current test stacks were stopped with their volumes retained; the earlier demonstrations remain preserved. See [current service evidence](../tools/office-current-rehearsal/results.json).

September 9 current-version recovery now independently passes five actual source Auth/PostgREST/Storage milestones and seven cold-restore milestones, plus 44 pure tests. One history-linked original was deliberately omitted from a restored target, correctly detected, and restored byte-for-byte; the 17-table RLS catalog, seven migrations, policies and 36 foreign keys matched. Both new projects are stopped with retained volumes and the frozen source is unchanged. An initial root-permission assumption required a reviewed continuation; source permissions and failed evidence were preserved. This is local physical recovery only; hosted capture/restore, off-device storage and operator acceptance remain required. See [current recovery evidence](../tools/office-current-restore/results.json).

September 8 local recovery also passed: complete cold database and file archives were restored into a separate fictional target. The verifier caught the intentionally absent file, then an absence-only correction restored its exact bytes. Logical records, policies, migration history and 36 foreign keys matched; the source volumes remained byte-identical. The rehearsal required guarded continuations after tool/network/response fixes, so it is not a clean one-shot timing benchmark. All 19 recovery guard tests pass. Hosted backup retention, off-device protection and operator acceptance remain open. See [actual restore evidence](../tools/office-restore/results-2026-09-08.md).

The [post-demo activation guide](office-hosted-activation.md) and [offline wiring tool](../tools/office-wiring/README.md) prepare consistent staff/member configuration and exact callbacks. The first staged packet leaves the public connection page unconfigured; hosted Auth signup policy remains a separate review. Configuration preparation does not activate the service.

## Included behavior

- Staff password recovery and invitation first-password setup use a separate `/admin/recovery.html` callback. The page verifies current staff access, removes callback credentials before loading the client and holds recovery state in memory. It never creates a staff role. Exact hosted callback configuration and real delivery still need rehearsal before staff activation.

- Office reliability pass: setup/current-role checks, persistent failed-load state, server-owned record versions, stable draft IDs, confirmed save results, interrupted-upload reconciliation, frozen pending submissions and event archive/restore. The final recovery migration is required before editing. A plain-language office guide explains daily work and recovery. See [reliability and recovery](office-reliability.md).

- Personal My follow-ups tracks each staff owner’s leadership contacts, recurring due dates, attempts/conversations, snoozes and pauses. An Overview panel and sidebar count show due reminders. A generic weekly calendar check-in can be exported and imported manually; no external alert channel or calendar account is connected. Apply the leadership migration and verify owner isolation before live use. See [leadership follow-up maintenance](leadership-followups.md).

- Office Overview offers due/overdue care plans, unassigned plans and monthly/quarterly coverage gaps as buttons that open the corresponding care queue. Counts describe loaded records and count each care role separately; failed or incomplete loads show unavailable counts rather than zero. Queue changes respect unsaved and uncertain drafts, and sign-out/revoked access clears the summary. People also opens care for the selected person, while a new-person review flags loaded records with the same normalized first/last names or a matching member identifier. This is a review prompt, not automatic merging or a guarantee against concurrent duplicates.

- Optional app signup/profile updates now have a prepared verified-email flow, private App signups review queue, Overview count and one-time welcome assignment after review. Independent deacon/teacher plans use three-month/monthly presets with coverage gaps and role-specific histories. The live signup service is unconfigured; the public page shows its email-form fallback. See [signup/care maintenance](signup-care-maintenance.md).

- Four private office sidebar editors cover announcements, committee contacts, Sunday slide links/files and prayer requests. Draft/ready status supports preparation; prayer sharing permission is recorded explicitly. These tools do not publish content or receive public prayer submissions automatically. The office-content migration is required after the membership/care migration; see [maintenance details](office-content-maintenance.md).

- Private membership/care expansion: richer People records, preserved history/corrections and page-photo review, guest intake, assigned deacon/contact intervals, visit logs and an on-screen follow-up queue. Authorized browser inspection verified Editor access to the original roll and 247 named rows on its visible tab. The full archive count and several source fields still need reconciliation; no real records were imported and no Sheet write-back is active. See [membership and care maintenance](membership-care-maintenance.md) and the [source field map](membership-source-reconciliation.md).

- Grow adds a Home feature and primary tab for the Habakkuk series: sourced historical context, chapter guide and reading questions. The pastor confirmed September 13 Colossians 3 and September 20 Habakkuk 1:2–4. September 20 is labeled scheduled, not preached; both dated notices expire appropriately in Central time. The unconfirmed September 6 history label is removed. [Songs @ the Creek](https://open.spotify.com/playlist/58ciuGp95u3Sq1bWVjSu13) is now saved publicly in the church Spotify account with 16 songs (1 hr 14 min) and connected to the candidate. Composition/recording screening follows the confirmed exclusions; see [selection record](music-selection.md). Click-only loading of the real embed, fallback and removal/focus were checked. Spotify’s playback timer advanced and was paused, but audible output and real-phone playback remain unverified. The pastor explicitly approved the [21-title book list](book-approval.md) for app inclusion on September 7, including The Spiritual Operator’s Manual and both Fee/Stuart coauthored guides. All 21 now appear in the candidate with title/author/topic search, short descriptions, expandable reading context and verified publisher/author links. Deployment was explicitly not authorized. The standalone review now includes 12 verified Amazon product destinations and 9 labeled searches, all untagged. Church-owned Associates signup is in progress; no tracking ID or commissions are active. Amazon links inside the PWA await eligibility clarification; see [Amazon preparation](amazon-book-links.md). See [Grow maintenance](grow-maintenance.md).

- The public app and website use identical `js/calendar-feed.js` consumers of a curated iCloud calendar. The app owns the dated public JSON/ICS snapshot; recurring events, exceptions, cancellations, Central time and exclusive all-day spans are handled by the pinned parser. Personal metadata and unreviewed titles are excluded. The user confirmed Yoga at 3:45 p.m. and WMU at 5 p.m. Automatic refresh is prepared in a read-only server adapter but not activated. See [calendar maintenance](calendar-maintenance.md).
- The app no longer promises an active weekly newsletter. Update preferences are explicitly an inquiry, not automatic mailing/texting-list enrollment. Connection and prayer forms validate locally and open email drafts. They retain values on the current page, never report receipt, and do not store or POST entries. A user can explicitly remove legacy app drafts without the code reading their contents.
- The service worker caches only the known public shell and validated public feeds. Feeds use network-first responses and saved offline status. Admin paths, unrelated navigation and private API responses are excluded. Only the exact configured public calendar endpoint may be cached after activation. An update waits for old app windows to close so unfinished forms are not forcibly reloaded. The `creek-v12` shell includes a persistent status panel above every tab: update-ready guidance survives offline/reconnect, and registration failure explains that offline access could not be confirmed. The panel never reads, stores or clears form contents. Browser connection reports do not establish that each external service is reachable.
- Watch is a native new-tab link to the configured YouTube live route, with a separate channel link for when no service is live. It does not claim a stream is currently running. Both online-giving and fallback copy omit the unverified year-end-statement promise; the giving destination is unchanged.
- Original fonts are served locally with their licenses. Phone routing, focus, text contrast and install-prompt behavior are reviewed.
- Creek Office provides private staff planning, membership/history and care records, categorized uploaded files and an audit log. Its calendar does not publish records to the public website. The authenticated interface explains separate Apple calendar editor invitations and links to iCloud. The calendar request sheet remains intake only; approved events must be entered in iCloud. Browser access to the original membership roll is verified; its import mapping and connection to the office remain pending.

## Reproducible local checks

From the app root, run `node --test tests/*.test.cjs` for public forms, shared calendar and service worker regressions. From `admin/tests`, run `npm ci --ignore-scripts` then `npm test` for the PostgreSQL role matrix and client session regressions. The latter uses PGlite and synthetic Auth/Storage structures; it does not validate a hosted service. See [staff test documentation](../admin/tests/README.md).

Browser checks use isolated local profiles and synthetic values where needed. Public drafts are never actually sent. Service-worker checks disable the browser network and verify a controlled reload, a reopened offline tab, validated saved public calendar data, local fonts/scripts and excluded admin/unrelated paths. A cached public calendar can miss a newly approved cancellation until reconnecting.

The current public suite passes **69 tests**, including 17 connection checks, Grow link validation, approved-book search, dated series behavior, eight app-status cases and the unchanged public cache tests. The staff suite passes **283 tests**, including 56 session/editor, 43 care, 33 membership-history, 25 office-content, 27 follow-up, 19 signup-review and 19 recovery-page checks. Late-response regressions use controlled fictional promises across timeouts; they do not prove server cancellation or durable browser recovery. No new hosted, native browser or physical-phone run is claimed for this pass.

Earlier local verification, retained as historical evidence: the iCloud converter has 17 synthetic tests; the HTTP adapter has 15, including real-converter integration. The integrated sample office passed desktop and 375/320-pixel viewport checks for edits, photo review, corrections, guest/care workflows, all four content editors, scrollable sidebar navigation and sign-out clearing. The adapter Deno entry type-checked with its pinned parser. These passes included verified signup/RPC isolation, versioned review and retry safety, calendar-month care roles, coverage gaps and both signup interfaces. Personal leadership verification added owner-only isolation (including other admins), stable create retries, immutable contact history, stale-save rejection, month/snooze calculations, calendar export and private dashboard clearing. Native review confirmed the personal module and editor at 375px, Overview at 320px, and a sample conversation advancing only its own plan. No actual calendar import/alert was exercised. The reliability pass added concurrency, lost-response, stalled-request, upload recovery, role/readiness and archive safeguards. Native review confirmed a fictional staff event archived and restored, a confirmed announcement edit, and the editor/office guide at 375px; the viewport was restored afterward. These viewport checks are not physical-phone evidence, and none contacted or deployed a church backend.

## Required before staff activation

The dedicated **Bluff Creek Church Office** project was created in Base1520 after approval of the $0/month creation quote. On September 8, separate explicit schema approval authorized all seven reviewed migrations. They are now applied and catalog verified: 17 RLS tables, 54 intended policies, 27 intended functions, one private file bucket, seven exact hosted/source SQL hash matches, both public profile-write grants paused and zero security-advisor findings. Do not replay the original filenames. See [dated hosted verification](office-hosted-verification-2026-09-08.md).

Two intended administrator identities are supplied privately, but Auth users/staff roles/records/storage remain empty. Source browser settings stay blank. The restricted staff candidate contains 45 files; the separately staged public follow-on contains 58. The older 49-file full builder output omits existing domain/brand assets and must not replace the repository. Next complete identity-bound staff setup, approved production email, exact hosted recovery callback and actual sign-in/role/file/recovery tests. The new [access preparation tool](../tools/office-access/README.md) passes 30 synthetic tests and creates no accounts or invitations. Auth signup remains separately unchanged. Establish off-device database and original-file restoration plus operator responsibility before real records. Staff activation and public release retain their own gates.

Uploaded spreadsheets are files to open/download; collaborative Sheet editing and synchronization are outside this version. Membership history and care require the additional migration and hosted access checks described in the setup guide. Photographed pages require manual transcription/review. On-screen care reminders are built; external follow-up messages and child check-in are not implemented. Optional verified-email self-registration is prepared but unconfigured; it creates no staff access and requires an approved reopening migration plus hosted email/redirect rehearsal before activation.

## Release order and rehearsal

Review and release the consolidated app PR before the companion website PR because the app owns the canonical curated calendar snapshot. Old overlapping P1 proposals should not be merged as a second calendar implementation. A specific ship instruction is required before any deployment or DNS change; preserve existing Google mail/MX configuration.

After separate backend approval, configure and verify the prepared public calendar adapter, including JSON/ICS output and the original feed held only as a server-side secret. With approved church staff, rehearse iCloud editor access, event changes/cancellations, a non-sensitive private file, real phone installation/reopening, mail-app delivery, YouTube stream/audio and the approved giving destination. These external journeys have not been completed by local automated tests. Record the deployed app/site revisions and current DNS before cutover so prior artifacts can be restored. Do not delete staff data as a rollback procedure.

## September 7 execution work

Separate website, public-app, private-office and membership checklists are now maintained in the shared Launch Execution Board and handoff. The public release can proceed independently of the full historical import when public information is accurate and inactive features remain explicit. Account/DNS changes and final release still need the concrete ship/setup instruction.

Local preparation now includes the read-only [office setup preflight](../tools/office-preflight/README.md) and [membership source preflight](../tools/membership-import/README.md). The office tool verifies the fixed SQL packet and declared setup contract, not hosted services. The membership tool validates the observed A:P source structure and reports sanitized exceptions; it never performs an import or marks identities/statuses approved. Its source walkthrough and future repeat-safe import writer remain open. The strengthened office tool passes 16 Node tests and 41 local checks; the membership tool passes 15 Python tests separately from the staff suite.

The pastor confirmed September 13 Colossians 3 and September 20 Habakkuk 1:2–4. The initial sermon-date pass passed 54 tests and native browser review of the dated labels and connection wording; the later September 7 public suite passed 57 tests. That checkpoint used service-worker shell `creek-v11`; the current September 9 totals and unchanged `creek-v12` shell are recorded above. Cached content changes wait for the normal app update lifecycle.

The authorized September 7 music pass created and reloaded the church-owned 16-song playlist, reconciled exact saved recordings, and connected it in the local Grow preview. No Spotify frame existed before the click; the actual playlist title appeared after loading; closing removed the frame and restored button focus. That music pass passed 54/54 public regression tests; the subsequent bookshelf pass passes 56/56. The saved public playlist is available now; the revised app remains an unmerged candidate.

The approved-books pass preserves all 21 approved IDs and Fee/Stuart coauthorship. Browser review at 375px verified the shelf, two Stuart matches, no-results recovery, clear-search focus and expanded fiction context. The separate review page now records approval instead of requesting it again. Affiliate enrollment, PWA Amazon eligibility and deployment remain distinct unfinished steps.

The next launch pass fixed iPadOS desktop-identity install guidance and aligned the app telephone URI with the website’s global destination. Six detector cases and telephone normalization were checked; public tests pass 56/56. Public website/app, giving, Watch and Maps endpoints responded, but actual-phone installation, audio, checkout identity and delivery remain unverified. See [visitor-journey evidence](visitor-journey-check.md). Fresh 51/51 SQL tests validate the moved migration paths. The initial full-suite stall was traced to a macOS dataless jsdom dependency and cleared by restoring the same locked packages. One initial-render timing assumption in the session test was replaced with a bounded condition wait; all immediate logout/privacy assertions remain synchronous and unchanged. The targeted session suite passes 28/28 and the full staff suite now passes 183/183 under Node 24.19.0, with unchanged dependency manifests. No hosted SQL or deployment ran.
