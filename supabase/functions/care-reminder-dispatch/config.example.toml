# Example for the later reviewed activation, not the active project config.
# Authentication is the dedicated server job key checked inside the handler.
[functions.care-reminder-dispatch]
verify_jwt = false
