# Local recovery evidence — September 8, 2026

**Result: `passed_local_restore_only`.** Seven actual milestone checks passed. The original source and restored target ended stopped with both database/Storage volume pairs preserved; cleanup was confirmed. This was a local synthetic rehearsal completed through reviewed continuations, not a clean first-attempt run, hosted deployment or production recovery-time commitment.

## What was recovered and verified

- Cold, complete PostgreSQL 17.6 and file-backed Storage volumes were captured after `pg_controldata` confirmed clean shutdown. Read-only, network-disabled helpers used the pinned database image; archive ownership, modes, paths, types and per-file hashes were validated.
- A distinct initially empty target received the complete database and Storage with exactly one observed payload omitted. Its prestart database manifest matched the original byte for byte, and its Storage manifest matched the deliberately incomplete archive.
- All 17 public-table inventories and row fingerprints matched, together with nine banned synthetic `.invalid` Auth users, nine identities, zero staff roles, private bucket/object metadata and all six unchanged migration rows. Relevant policies, grants, functions, all 36 validated foreign keys and the document-to-history-photo relationship matched; no orphaned foreign keys were found.
- The healthy target demonstrated the incomplete recovery: its database matched, while the private download returned HTTP 500/`InternalError`. A captured local Storage log identified `ENOENT` for the exact intentionally omitted on-disk payload. An arbitrary HTTP 500, authentication failure or another file's ENOENT is insufficient evidence.
- Only the absent payload was restored from the unchanged verified backup, using overwrite-refusing extraction. Complete Storage file hashes matched before the corrected target started. Afterward the private HTTP download returned the original 68-byte payload with the exact original SHA-256, while an anonymous public-file request remained denied. The final database/catalog/relationship fingerprints still matched.
- Fresh complete physical manifests of both original source volumes matched the immutable captured baseline at the end. The source configuration and volume identities remained unchanged. Both stacks were stopped without volume deletion, reset or trigger/schema changes.

The same six observed service image IDs and Supabase CLI 2.117.0 were used throughout. Docker published API, database and local-mail ports were checked as `127.0.0.1:55421`, `127.0.0.1:55422` and `127.0.0.1:55424`. No hosted project, SMTP provider, staff invitation or member account was activated.

## Reviewed corrections and preserved history

The first extraction attempt failed because Docker stdin was closed. Read-only evidence established that both newly created target volumes were still exactly empty and the source was unchanged. The write helper was corrected to use `--interactive`; two tiny network-isolated tmpfs-only checks confirmed BusyBox tar could restore root ownership/mode and fill an absent file. The original failed state was preserved before the bounded continuation performed the initial extraction.

The first target startup then could not reach the local published PostgreSQL port. The target used an internal bridge, while the proven working source used a normal bridge with loopback publishing. A private start-only diagnostic confirmed the refused local endpoint and stopped the target while retaining all data. A new run-labeled, loopback-bound bridge matching the source allowed the exact restored target to start healthy; the original internal network was retained. No database or Storage re-extraction was performed.

The first missing-file verifier expected only HTTP 400/404, but this pinned file backend returned HTTP 500. The resumed verifier required the exact missing payload's ENOENT evidence before accepting that response, then completed the one-file correction and full verification. All prior failed states and captured diagnostics were retained privately.

**Verification:** 19 pure guard tests passed. The exact-path guard additionally rejected prefix matches and unrelated files; the saved actual service evidence passed that guard. These tests accompany the actual service run rather than substituting for it.

## Timing and remaining boundaries

The final reviewed continuation took 99.435 seconds, including its preflight, two healthy-service observation pauses, correction, final checks and shutdown. The 898.277 seconds from initial successful extraction to final corrected verification includes failed startup, investigation and manual review. Neither number establishes a production RTO or an unattended first-run duration.

Raw database/Storage archives, row and file manifests, payload identifiers, local Auth state, API bodies and service/CLI logs remain outside Git in the private task bundle. The bundle directory is 0700 and private files are 0600; no raw keys or private records are included in this report. The retained target prevents a fresh runner invocation from silently reusing or overwriting it.

Production encrypted off-device backups, retention and monitoring, hosted key recovery, an operator-led restore, real-volume capacity and production restore compatibility remain separate readiness work. Supabase's database backups do not contain the uploaded Storage objects: [official backup boundary](https://supabase.com/docs/guides/platform/backups). This drill explicitly captured both volumes under PostgreSQL's stopped-cluster approach: [official filesystem-backup guidance](https://www.postgresql.org/docs/17/backup-file.html).
