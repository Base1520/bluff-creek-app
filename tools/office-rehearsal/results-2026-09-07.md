# Local office rehearsal — September 7, 2026

**38/38 local integration checks passed**, with **0 failed**, **0 blocked**, and **0 unconfirmed cleanup actions**. Status: `passed_local_http_only`. The separate harness guard suite passed **18/18** under Node **24.19.0**.

The successful run began September 7 at 8:11:44 p.m. America/Chicago and ended at 8:11:51 p.m. (`2026-09-08T01:11:44.354Z`–`2026-09-08T01:11:51.908Z`). It used the disposable `bcbc-office-rehearsal` stack: Auth/PostgREST/Storage at `http://127.0.0.1:55321` and the verified task database container bound to `127.0.0.1:55322`. The tested office capability revision was `20260907174301`, with the existing six migrations unchanged.

The stack operator piped local credentials in memory to the harness. The retained task evidence is the sanitized `work/office-http-rehearsal.json` in the parent Codex workspace. This summary contains no credentials, Auth sessions, signed URLs or person records.

## What passed

- The exact owned Unix socket, local database container and loopback binding were verified before fixture SQL. Five synthetic confirmed users signed in through real local Auth without requesting emails.
- Anonymous/nonstaff denial, forged user-metadata denial, actual admin/editor/viewer readiness, permitted base reads/writes, self-promotion denial, server-owned versions and stale-write protection passed through HTTP.
- Separate teacher/deacon plans, sensitive care/history visibility, preserved history, private announcement/prayer access and prayer-sharing approval passed.
- Leadership owner isolation, administrator cross-owner denial, versioned contact journaling and exclusion from shared audit passed.
- Real Storage upload, metadata, byte-for-byte private download, private-bucket/public-link denial, source-image visibility, wrong-owner upload rejection and linked-source preservation passed. An authorized short-lived signed URL worked without a session and was then rejected after expiry.
- Verified profile submission, owner-only profile retrieval, staff review, existing-person linkage without overwriting that record, one welcome plan, retry protection, profile-update review and new-visitor retry identity passed.
- Removing the editor's database role blocked new office/file access with the same unexpired session token. Other permitted staff still worked as a positive control.

## Initial bootstrap failure and correction

The first run reached local Auth successfully, then its service-key `staff_roles` insertion returned HTTP `403` / SQLSTATE `42501`. The local stack deliberately used `api.auto_expose_tables=false`, and inspection confirmed that `service_role` had no office table grants. Its ability to bypass RLS does not supply those grants.

This was an unsupported harness setup assumption, **not an observed product permission or schema bug**. Only fixture bootstrap/revocation/disposable-row cleanup changed: the helper now uses fixed local Docker/psql operations against registered synthetic UUIDs, matching `.invalid` Auth identities and explicit role/table allowlists. No migration, product schema, table grant, API exposure setting, trigger, or history rule was changed to make the test pass. Actual permission assertions continued using the anon key and signed-in user HTTP tokens.

The initial attempt's synthetic Auth user was signed out and banned; its denied staff-role insertion did not establish staff access. That earlier failed attempt remains historical evidence and is separate from the successful run.

## Cleanup and retained evidence

The successful run confirmed eight disposable-row/file cleanup operations as removed or absent. All five generated users had staff-role absence, session logout and a long ban confirmed. There were no uncertain cleanup actions.

Preserved synthetic contacts, events, historical entries, care and leadership records remain in the disposable stack, together with one linked source-image file and its metadata. The five successful-run Auth rows remain because preserved records/audit references may depend on them. Reported retained-ID inventories also include deliberately denied or uncertain insert attempts; they are not asserted database row totals. The harness did not reset the database, disable preservation rules, delete a bucket or touch fixtures belonging to another run.

## Subsequent local browser checks

Actual locally captured email journeys passed **8 password-reset checks**, **7 invitation/first-password checks**, and **7 public-signup checks**. The reset journey confirmed mismatch rejection, a changed password, rejection of the old password, manual login and reused-link denial. Invitation proved first-password setup, editor login and sign-out. Public signup proved the verification link, scrubbed callback, exactly one pending profile without staff access, and private-state clearing on reload.

All three distinct registered browser identities had staff-role absence and an Auth ban confirmed at cleanup; their isolated browser sessions were closed. Their preserved fictional Auth/history records remain separate from the five HTTP-harness identities. These checks used only captured local mail. No actual phone or hosted SMTP result is claimed.

## Gates still open

This establishes local service behavior at the recorded revision. Hosted setup and role/storage checks, production email delivery and recovery, actual phone and hosted browser journeys, database plus uploaded-file restoration, staff operator rehearsal and production acceptance remain separate gates. Assigned-deacon-only access is not created by this result, and already-issued signed URLs are not claimed to become invalid immediately when a staff role is revoked. No hosted project, live account, DNS setting or deployment was changed by this rehearsal.
