# Local office setup preflight

This checks that the checked-out office files form the reviewed setup packet. A passing result means the **local packet agrees**. It does not mean Creek Office is activated, a project exists, or real authentication, storage, permissions, email delivery or backups have been tested.

From the repository root, using Node.js 20 or later:

```sh
node tools/office-preflight/cli.mjs
node tools/office-preflight/cli.mjs --json
node --test tools/office-preflight/preflight.test.mjs
```

`--root LOCAL_CHECKOUT` checks another local checkout. There are no key, service URL, account or apply options. Exit codes are `0` for a passing local packet, `1` for a failed check, and `2` for invalid command options.

## What it checks

- The fixed SQL order in `manifest.json`, each predecessor, all twelve active migration filenames (including the base schema), unique 14-digit versions, ascending filename order, and the SHA-256 digest of each SQL file's bytes. Missing, changed or unlisted migrations fail.
- Required office scripts, styles, help, branding and font files are present and readable within the checkout.
- The client and explicitly named sixth migration declare the expected `office_readiness` revision, module names, function form and table prerequisite guard. These are static declaration checks; no SQL is executed and no live role is requested.
- The explicitly named seventh migration contains only the reviewed transaction revoking both public and private profile-submission signatures from PUBLIC, anon and authenticated. Dropping the pause from the manifest or restoring a submission grant fails this local check. Existing reads and staff review are preserved; Auth signup policy remains separate.
- The account guard, two applied direct-intake additions, guest-sheet projection and recoverable guest lifecycle match independently pinned SHA-256 values. Format 6 requires twelve files, ending with the exact two September 12 server-recorded filenames. The first ten entries and their readiness/asset metadata must match the byte-pinned frozen ten-file manifest. Omitting or weakening a required addition fails. These are source-byte checks, not hosted Auth, spreadsheet or delivery acceptance. Format 5, used by a separately prepared thirteen-file checker, is not accepted as this production history.
- The office configuration uses the expected three string-literal fields. It classifies empty/nonempty fields without evaluating JavaScript, extracting/decoding their contents, or including them in any result. Config files must still be read locally for this syntax/presence inspection. It does not inspect environment variables or credential stores.

It rejects file paths or symlinks escaping the checkout. It makes no network requests, invokes no account or database command, writes no application files, and never applies, concatenates or rewrites SQL. External assets are reported as untested.

## Configuration states

| State | Meaning | Local result |
| --- | --- | --- |
| `not_configured` | Project URL and publishable-key literals are both empty. | Can pass as a prepared candidate; activation remains outstanding. |
| `supplied_unverified` | Both literals contain characters. | Can pass as a prepared candidate; values and hosted behavior remain unverified. |
| `partial` | Only one of those two literals contains characters. | Fails; finish or clear configuration privately. |
| `unassessed` | Unsupported syntax, missing file, or unreadable configuration. | Fails; review configuration privately. |

A nonempty membership-sheet link is reported only as a boolean; it does not affect whether an office project is connected. Nonempty strings are not validated as URLs or keys, so even whitespace or an incorrect value can be `supplied_unverified`. The result never labels those values safe, valid, owned by the church, or activated. Do not paste credentials into the command line or preflight report. Secret/service-role keys do not belong in browser configuration.

## Reviewed SQL order

The current setup packet declares revision `20260907174301` and this explicit order:

1. `supabase/migrations/20260908220558_office_base.sql`
2. `supabase/migrations/20260908220613_membership_care.sql`
3. `supabase/migrations/20260908220623_office_content.sql`
4. `supabase/migrations/20260908220645_app_connections_care_roles.sql`
5. `supabase/migrations/20260908220658_leader_followups.sql`
6. `supabase/migrations/20260908220707_office_record_recovery.sql`
7. `supabase/migrations/20260908220718_pause_public_app_intake.sql`
8. `supabase/migrations/20260909124122_require_eligible_staff_auth_account.sql`
9. `supabase/migrations/20260910152017_direct_app_intake.sql`
10. `supabase/migrations/20260910152030_direct_intake_dispatch_extensions.sql`
11. `supabase/migrations/20260912125410_guest_sheet_snapshot.sql`
12. `supabase/migrations/20260912125438_guest_registration_lifecycle.sql`

The original preparation filenames were generated by Supabase CLI 2.117.0 in the required order, with the formerly separate base schema first. After the authorized hosted application, local filenames were aligned to the versions recorded by the connected Supabase migration tool. The original seven SQL files and approved SHA-256 values remain byte-identical. The eighth is an exact copy of the reviewed guard, aligned to its September 9 hosted version; see the [guard application record](../../docs/office-account-guard-hosted-2026-09-09.md). Its historical comments are retained without changing its hash. The seventh pauses the old `save_app_connection` signatures. The ninth adds separate authenticated, idempotent guest/prayer RPCs, private household fields, staff tasks and email outboxes. The tenth enables `pg_cron`/`pg_net` only; it creates no schedule or secrets. The readiness capability revision remains unchanged because staff read/review capabilities are retained. It does not prove the pause is installed: verify the seventh tracked version and actual privileges separately. The former format 4 placed the guard after the pause and the two intake additions afterward. Format 6 preserves that exact ten-file prefix, then requires the two recorded guest-register additions; it rejects duplicate versions and disagreement between dependency and filename order. See the [September 9 guard map](account-guard-hosted-history-2026-09-09.json), retained [September 8 history map](hosted-history-map.json) and unchanged historical [preparation map](migration-history-map.json) and [activation guide](../../docs/office-migrations.md). Historical comments inside the SQL retain their previous names. This local alignment does not repair or alter remote migration history. Compare current tracked and hosted versions before any new application; never replay either renamed baseline into a populated project.

The September 10 additions were applied with the approved SQL hashes and server-recorded timestamps. Their original CLI-generated review filenames remain untouched under `tools/direct-intake/migrations`; the active copies contain identical bytes, including historical draft comments. See the [September 10 history map](direct-intake-hosted-history-2026-09-10.json). Do not apply either review copy again or place duplicate review filenames into the active directory.

The two September 12 additions are exact copies of the reviewed guest-sheet and guest-lifecycle SQL, using the observed hosted versions `20260912125410` and `20260912125438`. The [September 12 history map](guest-register-hosted-history-2026-09-12.json) records their hashes and the dated read-only verification scope. All ten earlier SQL files and both review copies remain unchanged. The eleventh migration adds the service-only guest projection and staff settings; the twelfth adds versioned, recoverable guest removal and suppresses future claims for removed guest work. This source alignment performs no hosted query, migration replay, removal or restore. Fresh setup requires all twelve; an existing twelve-migration project requires verification, not replay.

The exact prior format-4 manifest is retained at [history/manifest-ten-2026-09-10.json](history/manifest-ten-2026-09-10.json), SHA-256 `0734cdc37022af4c946b2e4a8d93ad1b199093834f28373b1bf8c0c9497276ae`. Historical guest-lifecycle and removed-projection compatibility tests use that frozen input so their before/after and missing-prerequisite cases retain their original meaning. Deacon rotation, communication preferences, Office attention, their removed-guest compatibility change, weekly-email drafts and care-reminder planning are separate prepared packages; none enters the active twelve-file inventory. Their tests and proposed application scopes are not proof of an activated combined system.

The generic local-stack preparer consumes the current manifest and will copy all twelve files; copying alone does not establish database or service compatibility. The backup checker intentionally compares a capture's baseline fingerprint to the current manifest, so an older capture can fail `BASELINE_MISMATCH` without indicating corrupt payloads. Review older captures against their corresponding frozen source baseline. Existing service/restore tools retain their fixed historical schema and volume guards; do not repoint them or relabel their prior results as twelve-migration acceptance.

The exact former eight-file manifest is frozen at [history/manifest-eight-2026-09-09.json](history/manifest-eight-2026-09-09.json), SHA256 `572c8a631c293c1c2d322670b8f295de95e3baeb2fa59a82f2ae641e28812c68`. The direct-intake tests intentionally apply that baseline plus the functional SQL once; scheduler extensions require actual PostgreSQL and are not PGlite fixtures. The earlier staff-setup and old-intake-pause tests retain their eight-file scope. The original seven-file history and old service/restore harnesses remain unchanged and must not be repointed at newer schemas or retained volumes. Their historical counts do not certify a twelve-migration service or restore.

The unchanged `office_readiness` revision and its seventeen prerequisites cover the existing Office modules. New task/route support uses separate `direct_intake_readiness` capability flags; the local preflight does not contact either function.

After an intentional SQL change, review its dependencies and corresponding tests first. Then update the affected digest and, if needed, the manifest's order, expected revision, tables, modules and file inventory in the same reviewed change. There is no automatic digest-refresh command: refreshing hashes without reviewing SQL would hide a mismatch. The manifest explicitly identifies the readiness, old-intake pause, account guard, direct intake, dispatcher extensions, guest projection and guest lifecycle sources, and is a review record, not a signature or a security attestation. A future migration outside this packet should fail until explicitly incorporated.

## Remaining activation gates

Keep live acceptance separate from a local pass: compare the twelve recorded versions and exact hashes on the church project (never replay them into an existing project), verify browser-safe settings and approved auth redirects, viewer/editor/admin and revoked/nonstaff behavior, private uploads and signed links, denial through both old submission functions, authenticated direct guest/prayer RPCs and their private isolation, retained profile review, removal/restore, personal follow-up visibility and backup/restore. Any completed hosted or operator acceptance belongs to its dated evidence, not to this checker. The local preflight does not authorize or perform those actions, staff grants, training or a private pilot. It also does not test calendar synchronization, spreadsheet synchronization, One Call or other outbound delivery.

The regression suite uses synthetic temporary files and the current sanitized source packet. It checks blank/supplied configuration states, output redaction, unsupported executable syntax, digests, ordering, migration inventory, dependencies, readiness declarations, traversal/symlink rejection and CLI exit behavior. It does not connect to a real backend.

The original staff-first release kept the public connection page unconfigured. The SQL pause still applies to its old submission signatures and does not disable Auth account signup, sign-in, email delivery or database-owner/service access. The separately approved ninth migration supplies the current direct-intake path; local source configuration does not prove its hosted activation. Earlier six-file signup-success evidence remains historical.
